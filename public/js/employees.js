// Language: javascript
"use strict";

const currentDay = new Date().toISOString().slice(0, 10);

function employees() {
    // Hier später eine Funktion, die alle Mitarbeiter aus der Datenbank ausliest und in einer Liste anzeigt
    // Und danach durchcheckt, ob ein Mitarbeiter an "currentDay" einen Urlaub hat
    // Mit allPersons.forEach(function(entry) {} kann man durch alle Personen loopen
    // Mit allHolidays.forEach(function(entry){}) kann man durch alle Urlaube loopen
    // Kann auch beides in der employees.blade.php nachegschaut werden, wie die Variablen heißen
}